(function(){
  function initializeMobileNavigationPortal(){const nav=document.querySelector('.main-nav');const topbar=document.querySelector('.topbar');const right=document.querySelector('.topbar-right');if(!nav||!topbar)return;const update=()=>{const compact=window.matchMedia('(max-width: 1120px)').matches;if(compact){if(nav.parentElement!==document.body)document.body.appendChild(nav);nav.classList.add('mobile-nav-portal');return;}if(nav.parentElement===document.body){if(right&&right.parentElement===topbar)topbar.insertBefore(nav,right);else topbar.appendChild(nav);}nav.classList.remove('mobile-nav-portal');};update();window.addEventListener('resize',update,{passive:true});}
  function showView(name){document.querySelectorAll('.view').forEach(view=>view.classList.toggle('active-view',view.id===`view-${name}`));document.querySelectorAll('.nav-button').forEach(button=>button.classList.toggle('active',button.dataset.view===name));history.replaceState(null,'',`#${name}`);window.scrollTo({top:0,behavior:'smooth'});}
  function bind(){document.querySelectorAll('.nav-button').forEach(button=>button.addEventListener('click',()=>showView(button.dataset.view)));document.querySelectorAll('[data-go-view]').forEach(button=>button.addEventListener('click',()=>showView(button.dataset.goView)));}
  window.DriveOSNavigation=Object.freeze({initializeMobileNavigationPortal,showView,bind});
})();
