# Native tab concepts

Three built-in image_gen referenced-image mockups, September 4, 2026. Conceptual iOS 26 appearance, not native screenshots or pixel-exact implementation promises. Actual glass, dimensions and selection treatment belong to iOS. Existing screenshot content retained, including its historical dark thumbnail; live app already has the fixed light fallback. No app changes.

Common prompt:
Use case ui-mockup. Reference image 1 is existing JourneyDeck Home screen. Create a high fidelity portrait iPhone UI concept preserving all screen content above bottom navigation exactly. Replace ONLY bottom custom navigation with a plausible iOS 26 native Liquid Glass tab bar: one uninterrupted floating horizontal capsule, standard native height, modest left/right margins, five equal tab slots ALL on same baseline, no center bump, no protruding button, no neon colored outline, no custom large medallion. Labels left to right exactly Soundtracks, Memories, Home, Statistics, Settings. Home selected. Small native icon above each label: music note, photos, Home symbol per variant, chart, gear. Native subtle glass selection capsule, neutral warm ivory frosted glass surface, soft realistic system shadow, bottom safe area with home indicator. Keep native 25pt icon sizing and modest 10pt labels. Output one full portrait screenshot, no device frame, no annotations or headings. Reference 2 is logo guidance only; do not insert an app icon elsewhere.

## 01-plum-circle

Image: 01-plum-circle.png

Option 1: Plum Circle. Home uses a white house cutout inside a solid plum circle, the WHOLE symbol is only a standard 25pt native tab icon size. Native subtle selected capsule behind Home icon and label. Other icons muted gray-plum outline, selected Home label plum.

## 02-coral-home

Image: 02-coral-home.png

Option 2: Coral Home. Home uses a standard solid house.fill glyph in deep coral, same 25pt standard icon bounds as other icons, no surrounding circle. Native subtle selected capsule behind Home. Home label coral. Other icons muted gray-plum outline. Selection accent coral, glass background neutral ivory.

## 03-brand-signature

Image: 03-brand-signature.png

Option 3: Brand Signature. Use the circular pulse ring from reference image 2 as the small central Home tab icon, in plum-purple, with transparent/ivory negative spaces. No square icon tile or outer app-icon border. Same standard 25pt icon bounds, not raised or enlarged. Native subtle selected capsule behind Home. Label Home in plum, other icons muted gray-plum outline.
