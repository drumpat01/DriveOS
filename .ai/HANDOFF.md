# Current Handoff State

## Objective and working tree

- User wants Siri to speak answers from anywhere on the iPhone and Ask JourneyDeck to provide a typed, themed conversation inside the app. Both must use the same local answer service, with Apple Foundation Models interpreting questions when available and profile-scoped SQLite computing facts. The exact non-answer reply is `Beep Boop. Can not compute.`
- Branch `codex/v3-testflight-ota-20260925` contains the Build 39 source, later Ask/Siri and native crash fixes, and the five navigator avatars. The user requested that all non-ignored repository changes be committed and pushed to Git. Verify the resulting commit and remote state with Git. A separate archived release worktree at `e4fa3e5` was used for the earlier Ask chat OTA; no public App Review occurred.
- Changed paths include the Ask JS bridge/chat, native Ask service and Siri intent, focused tests, and this handoff. Verify with `git status` and path-scoped diffs before continuing.
- After installing Build 39, the user initially reported different in-app and Siri artist answers. Both JourneyDeck surfaces use the same native service/database. The post-Build-39 source changes prevent unspoken model time ranges and filters, ignore prior Siri context for standalone questions, and give common top-artist phrasings one direct local ranking plan. Planner revision is 4; these changes have not shipped.
- A later screenshot confirmed the Build 39 JourneyDeck Siri shortcut and in-app Ask both return AJR with 49 plays for the same question. The earlier Olivia Rodrigo reply was likely Siri answering outside the shortcut. Do not request another build for this mismatch; the post-Build-39 plan changes remain unshipped source.
- TestFlight crash incident `7C7689CB-6938-44F2-BD03-E26275B897C1` on Build 39, iPhone 16 Pro Max/iOS 27.2 beta: tester tapped Siri snippet's “Open supporting details” while outside the app. SIGBUS/FS page-in past EOF in a 32 KB mapped file, crashing thread in Expo SQLite `walFindFrame`; simultaneous main-thread native `AskArchive.rows` read the same master DB. Likely WAL shared-memory invalidation while the native Ask path repeatedly opened/closed a separate system SQLite connection; not proven without device reproduction. The native reader is now a single serialized process-lifetime connection, with transaction rollback on error so a failed read cannot poison later requests. No OTA or build has shipped this mitigation.
- User also confirmed Build 39 chat bubbles briefly appear then vanish when only the Ask sheet is closed and reopened. The existing reopen effect set `scrollRequested` for restored messages, triggering `scrollToEnd` during iOS formSheet size animation; the earlier empty-chat flash had the same native scroll symptom. The JS screen now leaves restored transcript at its natural position and scrolls only for new messages. This was published to Build 39's xprem `production` branch as iOS update `17903872178091`; physical-device confirmation remains.

## Current implementation

- `src/ask-journeydeck.ts` now calls `JourneyDeckAskService` directly for questions and ticket resolution; the separate Expo SQLite/local answer path is no longer used by the app. Siri's App Intent calls that same service. The service requests a Foundation Models query plan first when Apple's model is ready. The planner sees only the question and a bounded, record-free follow-up summary. The validated query engine uses allowlisted, read-only profile-scoped SQLite data after inference. Local rules answer supported questions if the model is unavailable or busy. Profile changes and device locking still invalidate native answers.
- Ask opens in a plain View with a two-sentence JourneyDeck greeting. The transcript ScrollView mounts only after a real message, removing the initial native auto-scroll path that blanked the screen in the user's recording. User questions and bot answers remain as chat bubbles in process memory across sheet closes and backgrounding. A reply in progress now survives sheet dismissal/reopening through a shared in-process chat session. Native in-app tickets persist for the process lifetime so follow-ups continue after five minutes; Siri spoken context still expires after five minutes. The user supplied the previously missing `scratchpad/navigator-icons` folder; Ask now maps the five theme IDs to its five transparent 256 px navigator avatars instead of app icons. This avatar change is local source only and has not been published.
- All non-answered Ask replies now use the exact response `Beep Boop. Can not compute.` in the in-app presentation and Siri dialog/snippet path; the chat's unexpected-request error bubble uses it too. Apple's own authentication prompt can still occur before the Siri intent runs.
- The screen reads the native Apple Intelligence availability and names the iPhone Settings action if it is off. Apps cannot enable Apple Intelligence programmatically; Apple's iOS model downloads are system managed. Current chat history is in memory only and ends when the app process ends.
- The original Ask prototype document contains historical details and is marked as superseded at the top.
- Build 39 review fixes: live-bundle marker guard in the native recorder; Siri details link uses the actual app scheme; earlier-iOS recorder shortcut availability; main-actor Siri recorder entry; model-fallback profile/epoch fence; native context expiration aligned with ticket lifetime; oldest-only ticket eviction; deterministic period follow-ups; explicit top-list sizes. See `mobile/recorder/docs/build-39-preflight.md`.
- Crash tests found startup recreating retired server jobs for native sessions. Native import now marks optional server completion unnecessary atomically; legacy job backfill excludes native sessions. No schema change.
- Candidate runtime is `3.0.0-preview.7`, isolated from Build 38 OTA. EAS `v3-testflight` pins the same verified Xcode 27 image as Build 38: `macos-tahoe-26.6-xcode-27.0`.

## Verification and release state

- Fresh `npm ci` completed in this worktree (real node_modules, no junction). Final mobile suite: 928 passed, zero failed, one optional golden-frame test skipped because private frames are absent. Typecheck, final V3 iOS export, config introspection, public privacy/support checks, and the authorized TestFlight gate pass.
- Online Expo Doctor is 19/20: its only finding recommends newer packages than the pinned SDK 58 preview. That installed preview's bundled dependency check passes; versions remain identical to the known Build 38 set. Do not suppress the advisory or silently upgrade 35 packages.
- `origin/main` fetched and verified at `5869905`; EAS confirms finished Build 38 `a34d2a2a-1201-4b9e-bcc3-e6eeb79a6968`. Schema remains 11. Build 39 Swift compilation passed; physical-device behavior remains pending.
- Build 39 is the current internal TestFlight native build (confirmed by the user), bundle `com.journeydeck.recorder`, runtime `3.0.0-preview.7`, schema 11. Build 38 was runtime `.6`. The latest published Ask chat OTA is iOS update `17903872178091`, publish group `d35090d9-b6ad-4870-a76d-5bc686904fbf`, message `AskChatBubblesVisible39`, release commit `e4fa3e5`; the server returned its compatible iOS manifest. The user has not yet confirmed its behavior on the phone.
- Build 39 also includes Siri membership-free access, all five light/dark icon pairs, and removal of SensitiveContentAnalysis from photo selection. TestFlight is never submitted to public App Review without separate authorization.
- Post-Build-39 Ask/Siri checks: 32 focused archive/query checks pass after the crash mitigation, TypeScript typecheck passes, and `git diff --check` reports only line-ending warnings. Windows cannot compile Swift, so the native connection change still needs cloud Xcode compilation and physical-device verification.
- Navigator-avatar change: five 256×256 transparent PNGs copied to `mobile/recorder/assets`; Ask UI maps dark, light, sakura, redline, and midnight-canopy to Cinematic Dark, Warm Ivory, Rosewater, Grand Touring, and Autumn Drive. Focused Ask UI checks 14/14, TypeScript, and diff check pass. No OTA or native build was requested for these assets.

## Next exact steps

- Build 39 FINISHED: EAS `79a20176-3607-42bb-99ea-766370d62172`, profile `v3-testflight`, live bundle, runtime `.7`. Xcode compilation/archive/signing passed. Signed IPA inspection confirms native identity, four App Shortcuts, five alternate icons, Foundation Models linkage, privacy manifest, and query resource. Details in `docs/build-39-preflight.md`; ignored receipts under root `artifacts/build39/`.
- Build 39 is available in internal TestFlight testing. ASC `submit:status` confirms 3.0.0 (39), runtime `.7`, `processingState: VALID`, `internalState: IN_BETA_TESTING`, existing app `6806502526`, EAS build `79a20176-3607-42bb-99ea-766370d62172`. EAS submission `001dc6db-a7b3-419d-9860-9767f8f519e0` says `ERRORED` only because its polling call got ASC 401 after it uploaded the complete IPA. The user confirmed it worked. Do not retry or rebuild. No public App Review requested.
- Candidate source receipt: ignored `artifacts/build39/source-manifest.json` (898 files), SHA256 `dc03303d7bd055118f0fca3bb8ebdfcbf7b9650ebde4a69c2a8898bb634e3ca4`. EAS includes uncommitted source; no staging, commit, or Git push occurred.
- The Ask chat OTA was published from a clean isolated Build 39 source snapshot plus the screen/test scroll fix. Focused UI tests 14/14, TypeScript, and iOS export passed. The pinned `eoas@3.2.2` CLI initially failed on Windows backslash asset names; its cached local asset-path normalization was patched, then the guarded `xprem:publish --target v3-testflight` succeeded. Docker Desktop, xprem, database, and tunnel were restarted and healthy. Keep Docker/tunnel running for update delivery. The separate post-Build-39 native crash mitigation and planner/query-engine changes were not included in the OTA.
- Physical Siri speech/model interpretation and iPhone/iPad acceptance remain for device testing; the binary is available to internal TestFlight testers. Do not use the stale EAS `ERRORED` status to initiate another upload.
- Next verification after a new native build: invoke the JourneyDeck shortcut explicitly with “Ask JourneyDeck,” ask “What is my most played artist?”, and compare its spoken answer with the exact same in-app question. Capture Siri's transcript/shortcut label if the answers still differ. No new build has been requested for this post-Build-39 fix.
- For the crash, review the retained-connection Swift patch, compile it with Xcode in a future authorized native build, then repeat Siri “Open supporting details” after the app has been backgrounded while Expo SQLite has active reads. Preserve the user's installed app and local data; do not recommend reinstalling or deleting SQLite files. The TestFlight crash connector required authentication, but the user supplied the symbolicated crash report directly.
